{
  "name": "rami",
  "version": "1.0.0",
  "description": "Read a story. Tell it back. Keep the lesson.",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Web app ready'",
    "dev": "python -m http.server 8000",
    "cap:build": "npm run build",
    "cap:ios": "cap copy && cap open ios",
    "cap:android": "cap copy && cap open android"
  },
  "keywords": ["stories", "learning", "reflection"],
  "author": "Yahya",
  "license": "MIT",
  "dependencies": {
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/filesystem": "^6.0.0",
    "@capacitor/preferences": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
